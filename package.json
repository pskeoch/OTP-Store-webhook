{
  "name": "webhook",
  "version": "1.0.0",
  "description": "Webhook app receiving order events from BigCommerce store",
  "main": "webhook.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemailer": "^6.3.0",
    "request": "^2.88.0"
  }
}
